---
name: ontologian:sync
description: Use when the user runs /ontologian:sync or wants to manually sync local .ontology/ to the global store.
---

Invoke the `sync` skill to sync the local `.ontology/` directory to the global store.
