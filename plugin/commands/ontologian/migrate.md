---
name: ontologian:migrate
description: Use when the user runs /ontologian:migrate or wants to split a domain's ontology.yaml into per-type files.
---

Invoke the `migrate` skill to split a domain's single `ontology.yaml` into separate type files.
