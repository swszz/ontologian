---
name: ontologian:validate
description: Use when the user runs /ontologian:validate or wants to check ontology YAML schema correctness and referential integrity.
---

Invoke the `validate` skill to check schema correctness and referential integrity across all domains.
